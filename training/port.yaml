# Port-surveillance training contract for a custom YOLO11 detector.
# Place images and YOLO txt labels under training/datasets/port before training.
path: training/datasets/port
train: images/train
val: images/val
test: images/test
names:
  0: small_boat
  1: cargo_vessel
